packages:
  "xlsx": "Excel file parsing for item uploads"
  "date-fns": "Date formatting and manipulation"
  "react-hook-form": "Form state management"
  "@hookform/resolvers": "Zod validation resolver for forms"
  "zod": "Schema validation"
  "lucide-react": "Icons"

images: []

notes:
  - Assumes @workspace/api-client-react exports generated react-query hooks and types.
  - Using crypto.randomUUID() for generating unique item IDs on the client side.
